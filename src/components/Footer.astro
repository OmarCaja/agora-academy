---
const year = new Date().getFullYear();
---

<footer class="site-footer">
    <div class="footer-content">
        <div class="footer-brand">
            <span class="footer-logo">ÁGORA</span>
            <p class="footer-tagline">Academia de matemáticas en Cuenca</p>
        </div>

        <nav class="footer-links" aria-label="Navegación pie de página">
            <a href="/">Inicio</a>
            <a href="/theory/algebra">Teoría</a>
            <a href="/exercises/1-eso">Ejercicios</a>
            <a href="/#contacto">Contacto</a>
        </nav>
    </div>

    <div class="footer-bottom">
        <p>© {year} Ágora Academy. Todos los derechos reservados.</p>
    </div>
</footer>

<style>
    .site-footer {
        margin-top: var(--spacing-xl);
        padding-top: var(--spacing-lg);
        padding-bottom: var(--spacing-lg);
        border-top: var(--border-width) solid var(--border-color);
        display: flex;
        flex-direction: column;
        gap: var(--spacing-md);
    }

    .footer-content {
        display: flex;
        flex-direction: column;
        gap: var(--spacing-md);
        justify-content: space-between;
        align-items: flex-start;
    }

    .footer-brand {
        display: flex;
        flex-direction: column;
        gap: 4px;
    }

    .footer-logo {
        font-weight: 700;
        font-size: 1.3rem;
        letter-spacing: 3px;
        color: var(--text-primary);
        border-bottom: 3px solid var(--accent);
        padding-bottom: 3px;
        align-self: flex-start;
    }

    .footer-tagline {
        font-size: 0.95rem;
        color: var(--text-muted);
        margin: 0;
    }

    .footer-links {
        display: flex;
        flex-wrap: wrap;
        gap: var(--spacing-md);
    }

    .footer-links a {
        color: var(--text-secondary);
        text-decoration: none;
        font-size: 0.95rem;
        font-weight: 700;
        padding-bottom: 2px;
        border-bottom: 2px solid transparent;
        transition: color var(--transition), border-color var(--transition);
    }

    .footer-links a:hover {
        border-bottom-color: var(--accent);
    }

    .footer-links a:hover {
        color: var(--accent-strong);
    }

    .footer-bottom {
        font-size: 0.85rem;
        color: var(--text-muted);
        border-top: 1px solid var(--rule-color);
        padding-top: var(--spacing-sm);
        margin-top: var(--spacing-xs);
    }

    .footer-bottom p {
        margin: 0;
    }

    @media (min-width: 768px) {
        .footer-content {
            flex-direction: row;
            align-items: center;
        }
    }
</style>
